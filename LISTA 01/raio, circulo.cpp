#include<stdio.h>
#include<stdlib.h>
#include<math.h>

/*Construa um algoritmo que calcule a área de um círculo cujo raio é fornecido pelo
usuário (use área= 3.14 x raio2)*/

int main (){
	float raio, area;
		printf(" Digite a area desejada: \n", raio);
		scanf("%f", &raio);
	
/*Calculo*/
area = (pow(raio,2) * 3.14);

	printf(" O valor da sua area e: %.2f.\n", area);
	
	
	
	
}
